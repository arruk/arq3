# Copie este arquivo para config/cross.env e ajuste os valores.

# Endereco SSH da DE10-Standard. O usuario padrao do BSP Terasic e root.
TARGET_HOST=root@192.168.1.50
TARGET_DIR=/home/root/chocolate-doom

# Toolchain ARM hard-float. Em Debian/Ubuntu, o prefixo abaixo corresponde ao
# pacote gcc-arm-linux-gnueabihf.
TARGET_TRIPLE=arm-linux-gnueabihf
CROSS_PREFIX=arm-linux-gnueabihf-

# Sysroot sincronizado da placa por scripts/cross/sync-sysroot.sh.
SYSROOT=

# Opcional e preferivel: environment-setup gerado pelo SDK Yocto/Terasic.
# Quando definido, o build carrega esse arquivo antes de configurar os fontes.
YOCTO_SDK_ENV=

# Cortex-A9 da DE10-Standard, ARMv7 hard-float com NEON.
TARGET_CFLAGS="-O2 -pipe -mcpu=cortex-a9 -mfpu=neon -mfloat-abi=hard"
JOBS=
